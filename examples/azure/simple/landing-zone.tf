module "azure_landing_zone" {
  source = "../../../azure"

  # TODO: fill in once azure/variables.tf is implemented
  # subscription_id = var.subscription_id
  # location        = var.location
  # prefix          = var.prefix
  # tags            = var.tags
}
